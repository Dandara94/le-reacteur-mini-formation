const hours = 3; // 3hours
const minute = 60; // 60s in 1 minute
const hour = 60; // 60min in 1 hour
const result = minute * hour; // multiply minute and hour
const total = total * hours;// multiply my result with the hours 
console.log(total);
// => 10800
